<h2><%= resource_name.to_s.humanize %>情報を編集</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>

　<p><%= image_tag @user.icon_url %></p>

  <div class="field">
    <%= f.label :name %>を変更する<br />
    <%= f.text_field :name, autofocus: true, autocomplete: "name" %>
  </div>

  <div class="field">
    <%= f.label :email %>を変更する<br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :password %>を変更する <i>(パスワードを変更しない場合空白のまま)</i><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> 文字以上</em>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :icon %>を変更<bt />
    <%= f.file_field :icon, autofocus: true, autocomplete: "icon" %>
  </div>

  <div class="field">
    <%= f.label :current_password %> <i>(更新のため、現在のパスワードを入力してください)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password" %>
  </div>

  <div class="actions">
    <%= f.submit "ユーザー情報を編集する" %>
  </div>
<% end %>

<h3>アカウントを削除する</h3>

<p>アカウントを削除する際は以下のボタンを押してください(一度削除すると元には戻せません) <%= button_to "アカウントを削除する", registration_path(resource_name), data: { confirm: "本当にアカウントを削除しても良いですか？" }, method: :delete %></p>

<%= link_to "戻る", :back %>
